# ==============================================================================
# VaudevilleRPG Configuration
# ==============================================================================

# Telegram Bot Token (get from @BotFather)
BOT_TOKEN=your-bot-token-here

# PostgreSQL connection string
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/vaudeville_rpg

# Debug mode (optional, default: false)
DEBUG=false

# ==============================================================================
# LLM Configuration (required for /generate_setting command)
# ==============================================================================

# LLM Provider: "anthropic" or "openai"
LLM_PROVIDER=anthropic

# API Key for the LLM provider
LLM_API_KEY=your-api-key-here

# Model to use (optional, defaults vary by provider)
# Anthropic: claude-sonnet-4-20250514
# OpenAI: gpt-4
LLM_MODEL=claude-sonnet-4-20250514

# ==============================================================================
# vLLM / Local Model Configuration
# ==============================================================================
# To use a local model with vLLM or compatible OpenAI-compatible server:
#
# LLM_PROVIDER=openai
# LLM_BASE_URL=http://localhost:8000/v1
# LLM_API_KEY=dummy
# LLM_MODEL=your-local-model-name
#
# Note: vLLM serves models at /v1 endpoint. The API key is required by the
# OpenAI SDK but not validated by vLLM, so any value works.

# ==============================================================================
# Admin Configuration
# ==============================================================================

# Comma-separated list of Telegram user IDs who have admin access
# These users can run /generate_setting even if not chat admins
# Get your ID from @userinfobot on Telegram
ADMIN_USER_IDS=123456789
